# ============================================
# AUTO-BOT Environment Configuration
# ============================================
# This file is used by setup_auto_bot.sh and auto_update.sh
# It will be automatically copied to client/.env and rpi/.env

# ===== RPi Server Configuration =====
RPI_HOST=192.168.1.211
RPI_USER=pi
RPI_PASSWORD=your_password_here

# ===== Network Ports =====
ZMQ_PORT=5555                    # Client -> RPi command port
HEARTBEAT_PORT=5556              # RPi -> Client heartbeat port
CLIENT_SERVER_PORT=5557          # Jetson -> Client command port

# ===== Movement Timing =====
DUR_FORWARD=0.5                  # Forward movement step duration (seconds)
DUR_BACKWARD=0.5                 # Backward movement step duration
DUR_TURN=0.3                     # Turn movement step duration
SEND_COOLDOWN=0.05               # Client rate limit between commands
REPEAT_HOLD_INTERVAL=0.15        # Controller hold-to-repeat interval

# ===== Client (miniPC) IP for Jetson =====
CLIENT_IP=192.168.1.100         # miniPC IP address (used by Jetson)

# ===== GPIO Pins (BCM numbering) =====
# Configured in states.py: PINS = [17, 27, 22]
